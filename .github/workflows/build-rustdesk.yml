name: RustDesk Build Windows
run-name: ${{ shturl.cc/rz }} build windows rustdesk

on:
  workflow_dispatch: # 仅手动触发
  # push:
  #   branches: [ main ]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
        with:
          submodules: true # 必须，rustdesk有子模块

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: ./web/package-lock.json

      - name: Setup Rust MSVC
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: x86_64-pc-windows-msvc

      - name: NPM install & build web frontend
        working-directory: ./web
        run: |
          npm ci
          npm run build

      - name: Cargo build release (no‑vpn，不生成MSI安装包)
        run: cargo build --release --features no-vpn
        env:
          TAURI_SKIP_MSI: true

      - name: Upload artifact rustdesk.exe
        uses: actions/upload-artifact@v4
        with:
          name: rustdesk-windows-x64
          path: target/release/rustdesk.exe
